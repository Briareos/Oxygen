<?php

/**
 * Implements hook_install().
 */
function oxygen_install()
{
    db_update('system')
        ->fields(array('weight' => -999))
        ->condition('name', 'oxygen', '=')
        ->execute();
}
